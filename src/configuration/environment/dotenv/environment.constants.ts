/**
 * Configuration object for environment-related paths and settings
 */
export const ENVIRONMENT_FILE_CONFIG = {
  ROOT_DIRECTORY: "envs",
  BASE_ENV_FILE: ".env",
  SECRET_KEY_PREFIX: "SECRET_KEY",
} as const;
